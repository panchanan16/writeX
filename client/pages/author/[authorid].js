import AuthorPro from "@/components/ui/authorUI/authorProfile"

export default function AuthorProfile() {
    return (
        <section style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center' }}>
            <AuthorPro />
        </section>
    )
}